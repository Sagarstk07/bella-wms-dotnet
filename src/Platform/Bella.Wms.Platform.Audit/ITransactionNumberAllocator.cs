// OBSOLETE 2026-09-04. Replaced by Bella.Wms.Platform.Data.ISequenceAllocator.
// 51 of the 55 sequences in irms.df cycle, not just transactions_trans_num, so the
// allocator was generalised out of the Audit project. This file is emptied rather than
// deleted because the file bridge cannot delete. DELETE IT FROM DISK AND FROM GIT.
